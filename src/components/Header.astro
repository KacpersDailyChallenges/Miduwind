---
interface HeaderProps {
    image: string;
    alt: string;
    username: string;
    accentColor: string;
    setMonospace: boolean;
    setVerified: boolean;
}

import { header } from "@/data.json";
import VerifiedIcon from "@/assets/VerifiedIcon.svg";

const { image, alt, username, accentColor, setMonospace, setVerified } =
    header satisfies HeaderProps;
---

<section class="flex flex-col items-center justify-center pt-4">
    <div class="relative">
        <!-- Pomarańczowe koło wystające 1 rem (4 x 0.25 rem) dookoła -->
        <div class="absolute -inset-4 rounded-full bg-orange-500"></div>
        <img
            src={image}
            alt={alt}
            class="relative w-70 rounded-full mask-b-from-90%"
        />
    </div>
    <span
        class=`flex gap-1 px-3 py-0.5 -mt-4 header-custom z-1 bg-black/60
        text-lg border-1 rounded-xl ${setMonospace ? "font-mono": "" }`
    >
        {username}
        {setVerified && <VerifiedIcon />}
    </span>
</section>

<style define:vars={{ accentColor }}>
    .header-custom {
        color: var(--accentColor);
        border-color: var(--accentColor);
    }
</style>
